// import { makeSelectCharacterDomain } from '../selectors';

// const selector = makeSelectCharacterDomain();

describe('makeSelectCharacterDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
